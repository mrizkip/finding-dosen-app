<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skripsi.mrizk.findingdosen">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.mainMahasiswa.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.login.LoginActivity" />
        <activity android:name=".main.register.RegisterActivity" />
        <activity android:name=".main.profilDosen.ProfilDosenActivity" />
        <activity android:name=".main.myProfile.MyProfileActivity" />
        <activity android:name=".main.editProfil.EditProfileActivity" />
        <activity android:name=".main.posisiDosen.PosisiDosenActivity" />
        <activity android:name=".main.mainDosen.MainActivityDosen" />
        <activity android:name=".main.editStatus.EditStatusActivity" />
        <activity android:name=".main.StatusAktifActivity" />
        <activity android:name=".main.StatusTidakAktifActivity" />
        <activity android:name=".main.splashScreen.SplashScreenActivity"></activity>
    </application>

</manifest>